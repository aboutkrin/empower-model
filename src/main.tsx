import React from 'react'
import ReactDOM from 'react-dom/client'
import EmpowerModel from './EmpowerModel'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
        <EmpowerModel />
    </React.StrictMode>
)
