build:
  config:
    NODE_ENV: production
    NPM_CONFIG_PRODUCTION: false
    NODE_MODULES_CACHE: false
  packages:
    - nodejs-20.x
  scripts:
    build: npm run build
    start: npm run preview -- --host 0.0.0.0 --port $PORT 